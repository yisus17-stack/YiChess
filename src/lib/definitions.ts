export type NavLink = {
  href: string;
  label: string;
};

export type ChessPiece = {
  name: string;
  description: string;
  imageUrl: string;
  imageHint: string;
};

export type SearchableContent = {
  title: string;
  path: string;
  description: string;
};
